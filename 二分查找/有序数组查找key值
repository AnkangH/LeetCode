/*
题目：给定一个有序（不减）数组，查找其中是否有target元素
解析：因为数组有序，所以采用二分查找。
l，r分别指向数组第一个和最后一个元素。
*/
int find(vector<int> v, int l, int r, int target)
{
	int res = -1;
	while (l <= r)
	{
		int mid = l + (r - l) / 2;
		if (v[mid] == target)
			return mid;
		else if (v[mid] > target)
			r = mid - 1;
		else
			l = mid + 1;
	}
	return res;
}
